package com.rafa.mi_bolsillo_app.ui.settings.theme

/**
 * Enum que define las opciones de tema disponibles para la aplicación.
 * Las opciones incluyen LUZ (LIGHT), OSCURO (DARK) y SISTEMA (SYSTEM).
 */

enum class ThemeOption {
    LIGHT,
    DARK,
    SYSTEM
}