package com.rafa.mi_bolsillo_app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Nueva paleta de colores rediseñada y simplificada para la aplicación.
 */

// Colores base para la marca y la UI
val BrandBlue = Color(0xFF073763)
val BrandGray = Color(0xFFE0E0E0)
val BrandWhite = Color(0xFFFFFFFF)
val BrandBlack = Color(0xFF121212) // Usado para el fondo oscuro


// Colores de Texto
val TextPrimaryDark = Color(0xFF212121) // Texto principal sobre fondos claros
val TextPrimaryLight = Color(0xFFE0E0E0) // Texto principal sobre fondos oscuros
val TextSecondary = Color(0xFF757575)   // Texto secundario en ambos temas

// Colores de Superficie y Fondos
val LightBackground = Color(0xFFFAFAFA)
val DarkSurface = Color(0xFF1E1E1E)      // Para tarjetas en modo oscuro

// Colores Funcionales
val IncomeGreen = Color(0xFF4CAF50)
val ExpenseRed = Color(0xFFD32F2F)

// Colores UI Secundarios
val LightGray = Color(0xFFBDBDBD)     // Para FAB en modo oscuro y outlines